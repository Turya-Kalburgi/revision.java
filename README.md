# revision.java

A simple Java practice repository containing beginner-level programs.

Includes examples such as Fibonacci, factorial, palindrome checks, prime tests, array sums, and other basic exercises.

## Running a program

Compile with:

    javac ProgramName.java

Run with:

    java ProgramName

Replace `ProgramName` with the Java file name (without `.java`).
